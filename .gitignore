node_modules/

# Except the door agent's, which is vendored on purpose: koffi ships inside
# the download so a shop PC needs nothing fetched over a counter's wifi.
!agent/node_modules/
*.log
.env
.env.*
.vercel

# Output from the document render harness — screenshots and the page they
# come from, written into the repo root by a script run from here.
/docs.html
/cof.png
/inv.png
/pack.png
